*CD itparc
      COMMON/ITPARC/DVELIT(8,5),HWEFIT
      REAL DVELIT,HWEFIT
C
#if defined(DOC)
      DVELIT - Polynomial drift coefficients per layer.
      HWEFIT - Wire inefficiency at theta=90 degrees.
#endif
