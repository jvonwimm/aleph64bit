      COMMON/MCG1DA/XCENTR(NSLEND),YCENTR(NSLEND),XWIDTH(NSLEND), YWIDTH
     +(NSLEND),ZENCAP(NSLEND),THICKC
#if defined(DOC)
C
C     endcap muon chambers data
C
C       XWIDTH(NSL) = length of module in X - direction
C       YWIDTH(NSL) =   "             "  Y
C       XCENTR(NSL) = X  of center of module (aleph-system)
C       YCENTR(NSL) = Y                "
C       ZENCAP(NSL) = inner Z coordinate of module (aleph-system)
C       THICKC      = thickness of module
C
#endif
