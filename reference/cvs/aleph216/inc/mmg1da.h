      COMMON/MMG1DA/ZWIDTM(NSLMDA),WIDTHM(NSLMDA),CENDVM(NSLMDA), RINTEM
     +(NSLMDA),INPHIM(NSLMDA),ZMODUL(NSLMDA),THICKM
#if defined(DOC)
C
C           middle angle muon chambers data
C
C       ZWIDTM(NSLOT) = module width along z axis
C       WIDTHM(NSLOT) = width(in r-phi) of module
C       CENDVM(NSLOT) = offset of slot center respect normal from origin
C       RINTEM(NSLOT) = inner radius of module
C       INPHIM(NSLOT) = phi index of module
C       ZMODUL(NSLOT) = inner z coordinate of module
C       THICKM        = thickness of module
C
#endif
