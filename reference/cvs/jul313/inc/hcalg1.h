      COMMON/HCALG1/HT0LAY,HTSLAB,HCTGAP,HXBMIN,HXBMAX,
     + HREMIN,HREMAX,HREMED,HZEMIN,HZBMIN,HTSAMP,HTHCLE,
     + HYBMIN,HYBMAX,HZEMAX,HZBMAX
#if defined(DOC)
C HT0LAY = iron thickness in front of first barrel layer
C HTSLAB = iron slab thickness in Hcal
C HCTGAP = air gap between slabs
C HTSAMP = HTSLAB+HCTGAP = sampling thickness
C HXBMIN = barrel radius at first slab entrance
C HXBMAX = max barrel radius (inner tangent)
C HTHCLE = max barrel radius - min barrel radius (HXBMAX-HXBMIN)
C HREMIN = inner endcap min radius
C HREMED = inner endcap max radius
C HREMAX = outer endcap max radius
C HYBMIN = barrel module width at first slab entrance
C HYBMAX = barrel module width at last  slab exit
C HZEMIN = endcap min zeta
C HZEMAX = endcap max zeta
#endif
